package models.components.global.leftsidemenu;

import org.openqa.selenium.WebDriver;

public class ManufacturerMenuComponent {
    private final WebDriver driver;

    public ManufacturerMenuComponent(WebDriver driver) {
        this.driver = driver;
    }
}
